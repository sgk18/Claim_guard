{
  "commands": [
    "echo NGINX_STATUS: $(systemctl is-active nginx)",
    "echo SERVER_STATUS: $(systemctl is-active claimguard-server)",
    "echo WEB_STATUS: $(systemctl is-active claimguard-web)",
    "curl -s -i http://127.0.0.1/healthz | head -n 5",
    "curl -s -i http://127.0.0.1/server-health | head -n 5",
    "curl -s -i http://127.0.0.1/api/v1/claims | head -n 10"
  ]
}
